import { CardType } from '../models/CardType';

interface CardProps {
	cardData: CardType;
	isFlipped: boolean;
}
export default function Card({ cardData, isFlipped }: CardProps) {
	return (
		<div>
		</div>
	);
}
